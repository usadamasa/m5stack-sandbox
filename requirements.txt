# Install with uv, not pip. Inside the Claude Code sandbox pip's truststore
# cannot reach trustd and fails with SSLCertVerificationError OSStatus -26276;
# uv is in sandbox.excludedCommands and works.
#
#   uv venv tmp/m5venv
#   uv pip install --python tmp/m5venv/bin/python -r requirements.txt

esptool==5.3.1
pyserial==3.5

# MCP SDK. v2.0.0 dropped mcp.server.fastmcp — host/buddy_mcp.py uses
# MCPServer from mcp.server.mcpserver instead. Pinned because that import
# path moved between majors.
mcp==2.0.0
