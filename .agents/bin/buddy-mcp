#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(dirname -- "$(dirname -- "$script_dir")")

exec "$repo_root/.venv/bin/python" "$repo_root/host/mcp/src/buddy_mcp.py"
